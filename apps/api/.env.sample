# Database connection string
DATABASE_URL="postgresql://username:password@localhost:5432/playaplan?schema=public"

# JWT Authentication
JWT_SECRET="your_jwt_secret_key"
JWT_EXPIRATION_TIME="24h"

# Server configuration
PORT=3000
NODE_ENV="development" # development, production, test

# Application settings
APP_BASE_URL="http://localhost:3000"
APP_NAME="PlayaPlan"

# Frontend configuration
FRONTEND_URL="http://localhost:5173"

# Email service configuration
EMAIL_PROVIDER="sendgrid" # sendgrid or smtp
EMAIL_FROM="noreply@example.com"

# SendGrid Configuration
SENDGRID_API_KEY="your_sendgrid_api_key"

# SMTP Configuration (used if EMAIL_PROVIDER=smtp)
MAIL_HOST="smtp.example.com"
MAIL_PORT=587
MAIL_USER="username"
MAIL_PASSWORD="password"
MAIL_SECURE=false # true for 465 port, false for other ports
MAIL_IGNORE_TLS=false # set to true to ignore TLS
MAIL_REQUIRE_TLS=false # set to true to require TLS
MAIL_DEBUG=false # outputs SMTP traffic logs when true

# Payment providers
# Stripe
STRIPE_SECRET_KEY="your_stripe_secret_key"
STRIPE_PUBLIC_KEY="your_stripe_public_key"
STRIPE_WEBHOOK_SECRET="your_stripe_webhook_secret"

# PayPal
PAYPAL_CLIENT_ID="your_paypal_client_id"
PAYPAL_CLIENT_SECRET="your_paypal_client_secret"
PAYPAL_MODE="sandbox" # sandbox or live

# Other settings
DEFAULT_ADMIN_EMAIL="webadmin@example.com"
DEFAULT_ADMIN_PASSWORD="change_this_in_production"
